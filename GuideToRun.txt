# Backend
cd backend
cp .env.example .env   # add your MongoDB URI
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python TestDataset.py  # optional sample data
uvicorn main:app --reload  # runs on :8000

# Frontend  
cd frontend
npm install
npm run dev            # runs on :5174